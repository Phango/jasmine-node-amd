# Simple example
cd to /simple
Run:
```bash
jasmine-node --color --autotest --runWithRequireJs .
```

# Component example
cd to /components

if first time run
```bash
npm install
```

To run tests:
```bash
karma start
```
